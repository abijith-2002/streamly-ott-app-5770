<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/exo_controller"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="0dp">

    <!-- Central play/pause control; StyledPlayerControlView will set icon state -->
    <ImageButton
        android:id="@+id/exo_play_pause"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_gravity="center"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/watch_now"
        app:tint="@color/streamly_on_surface" />

    <!-- Bottom controls area: stacked rows -->
    <LinearLayout
        android:id="@+id/bottomControls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:paddingStart="@dimen/player_controls_side_padding"
        android:paddingEnd="@dimen/player_controls_side_padding"
        android:paddingBottom="@dimen/player_controls_bottom_padding">

        <!-- Row 1: Action buttons aligned to the right (Settings then Fullscreen) -->
        <LinearLayout
            android:id="@+id/actionButtons"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end|center_vertical"
            android:layout_marginBottom="8dp">

            <ImageButton
                android:id="@+id/exo_settings"
                android:layout_width="@dimen/action_button_size"
                android:layout_height="@dimen/action_button_size"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/settings"
                app:tint="@color/streamly_on_surface"
                android:contentDescription="@string/player_settings"
                android:layout_marginEnd="8dp" />

            <ImageButton
                android:id="@+id/exo_fullscreen"
                android:layout_width="@dimen/action_button_size"
                android:layout_height="@dimen/action_button_size"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/fullscreen"
                app:tint="@color/streamly_on_surface"
                android:contentDescription="@string/player_fullscreen"
                android:visibility="visible"
                android:alpha="1.0"
                android:minWidth="@dimen/action_button_size"
                android:minHeight="@dimen/action_button_size" />
        </LinearLayout>

        <!-- Row 2: current time | progress bar | total duration (as a single group, slightly lower) -->
        <LinearLayout
            android:id="@+id/progressRow"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="4dp">

            <!-- Current position -->
            <TextView
                android:id="@+id/exo_position"
                style="@style/TextAppearance.Figtree.Label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="00:00"
                android:textColor="@color/streamly_on_surface" />

            <!-- Progress container with rounded clipping (responsive width) -->
            <FrameLayout
                android:id="@+id/progressContainer"
                android:layout_width="0dp"
                android:layout_height="@dimen/timebar_touch_target"
                android:layout_weight="1"
                android:layout_marginStart="@dimen/player_timebar_side_margin"
                android:layout_marginEnd="@dimen/player_timebar_side_margin"
                android:minWidth="@dimen/player_timebar_min_width"
                android:background="@drawable/bg_timebar_rounded"
                android:clipToOutline="true">

                <!-- Progress bar (ExoPlayer TimeBar) -->
                <com.google.android.exoplayer2.ui.DefaultTimeBar
                    android:id="@+id/exo_progress"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@drawable/bg_timebar_rounded"
                    android:clipToOutline="true"
                    app:bar_height="@dimen/timebar_height"
                    app:touch_target_height="@dimen/timebar_touch_target"
                    app:played_color="@color/streamly_on_surface"
                    app:buffered_color="#99FFFFFF"
                    app:unplayed_color="#66FFFFFF"
                    app:scrubber_color="@color/streamly_on_surface"
                    app:scrubber_enabled_size="10dp"
                    app:scrubber_dragged_size="12dp" />
            </FrameLayout>

            <!-- Total duration -->
            <TextView
                android:id="@+id/exo_duration"
                style="@style/TextAppearance.Figtree.Label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="00:00"
                android:textColor="@color/streamly_on_surface" />
        </LinearLayout>

    </LinearLayout>

</FrameLayout>
